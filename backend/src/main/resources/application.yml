# ═══════════════════════════════════════════════════════════════════════════════
# 🎓 APPLICATION.YML - Configuration de l'application
# Analogie JS: C'est comme un fichier .env combiné avec la config Express
# ═══════════════════════════════════════════════════════════════════════════════

spring:
  application:
    name: service-pulse
  
  # ─────────────────────────────────────────────────────────────────────────────
  # 💾 Configuration de la base de données H2 (en mémoire)
  # Analogie JS: Comme configurer Sequelize avec SQLite
  # ─────────────────────────────────────────────────────────────────────────────
  datasource:
    url: jdbc:h2:mem:servicepulse    # Base en mémoire nommée "servicepulse"
    driver-class-name: org.h2.Driver
    username: sa                      # Utilisateur par défaut H2
    password:                         # Pas de mot de passe en dev
  
  # Console H2 pour visualiser la base (comme phpMyAdmin mais intégré)
  h2:
    console:
      enabled: true                   # Accessible sur http://localhost:8080/h2-console
      path: /h2-console
  
  # ─────────────────────────────────────────────────────────────────────────────
  # 🔄 Configuration JPA/Hibernate
  # Hibernate = L'implémentation de JPA (comme Sequelize implémente l'ORM)
  # ─────────────────────────────────────────────────────────────────────────────
  jpa:
    hibernate:
      ddl-auto: create-drop           # Recrée les tables à chaque démarrage (dev only!)
    show-sql: true                    # Affiche les requêtes SQL dans la console
    defer-datasource-initialization: true  # Exécute data.sql APRÈS la création des tables
    properties:
      hibernate:
        format_sql: true              # Formate le SQL pour lisibilité
  
  # Exécute data.sql à chaque démarrage
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql

# ─────────────────────────────────────────────────────────────────────────────
# 🔐 Configuration JWT
# ─────────────────────────────────────────────────────────────────────────────
application:
  security:
    jwt:
      secret-key: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
      expiration: 86400000 # 24 heures en millisecondes

# ─────────────────────────────────────────────────────────────────────────────
# 🌐 Configuration du serveur
# ─────────────────────────────────────────────────────────────────────────────
server:
  port: 8080                          # Port du serveur (comme app.listen(8080))

# ─────────────────────────────────────────────────────────────────────────────
# 📊 Configuration Actuator (Monitoring)
# ─────────────────────────────────────────────────────────────────────────────
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics  # Endpoints exposés
  endpoint:
    health:
      show-details: always            # Affiche les détails du health check

# ─────────────────────────────────────────────────────────────────────────────
#  Configuration des logs
# ─────────────────────────────────────────────────────────────────────────────
logging:
  level:
    com.servicepulse: DEBUG           # Nos logs en mode DEBUG
    org.springframework.security: DEBUG
